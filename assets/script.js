var formControlRange = document.getElementById ("formControlRange");
var formControlNumber = document.getElementById ("formControlNumber");

formControlRange.addEventListener("input", syncRange);
formControlNumber.addEventListener("input", syncRange);

function syncRange(a) {
     
}